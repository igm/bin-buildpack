#!/usr/bin/env bash
# bin/release <build-dir>

location=$1

# echo >&2 "build location: $location"

manifest="default_process_type:
  web: echo >%2 "$(ls)"
"

# echo >&2 "$manifest"

echo "$manifest"
