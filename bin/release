#!/usr/bin/env bash
# bin/release <build-dir>

location=$1

# echo >&2 "build location: $location"

manifest="config_vars:
default_process_type:
  web: /home/vcap/app/agent
"

# echo >&2 "$manifest"

echo "$manifest"
