#!/usr/bin/env bash

# set -eo pipefail

var=$(pwd)
echo >&2 "The current working directory $var."

ls=$(ls)
echo >&2 "$ls."

echo >&2 "Compiling... "

mkdir -p "$1" "$2"
build=$(cd "$1/" && pwd)
cache=$(cd "$2/" && pwd)


cp ../agent $build
echo >&2 "Compilation done."
